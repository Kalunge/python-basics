sum = 0
for num in range(1000, 2000):
    if num % 2 == 0:
        sum += num

print(sum)

# declare a variable called sum or total SUM = 0
# we generate numbers using the range function RANGE(MIN_LIMIT, MAXIMUM_LIMIT)
# we check for even numbers NUMBER % 2 == 0
# we add each even number to the sum/total SUM = SUM + NUMBER
# print the sum/total PRINT(SUM)